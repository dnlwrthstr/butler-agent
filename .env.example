# Copy to .env (or Butler Agent/task/agent/.env) and fill in your values.
# The agent talks to an OpenAI-compatible endpoint (LiteLLM proxy or api.openai.com).
LITELLM_API_KEY=your-api-key
LITELLM_BASE_URL=https://api.openai.com/v1
